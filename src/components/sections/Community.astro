---
/**
 * Community.astro
 * Community & Professional Interests section.
 */

const communities = [
  { name: 'A11y North', focus: 'Accessibility' },
  { name: 'Queer Tech Leeds', focus: 'LGBTQ+ in tech' },
  { name: 'Coding Black Females', focus: 'Black women in tech' },
  { name: 'Women in Tech York', focus: 'Women in tech' },
  { name: 'Cajigo Women in Tech', focus: 'Women in tech' },
  { name: 'NurtureMe Mentoring', focus: 'Diversity in tech mentoring' },
  { name: 'AI Workshop', focus: 'AI & data learning' },
];
---

<section class="community section" id="community" aria-labelledby="community-heading">
  <div class="container">

  <div class="section-header">

  <h2 class="section-header__title" id="community-heading">
    Tech communities and mentoring
  </h2>

  <div class="divider" aria-hidden="true"></div>

  <p class="section-header__description">
    I learn with communities that make technology more accessible, inclusive and supportive.
    I also take part in mentoring, shared learning and communities of practice.
  </p>
</div>

    <ul class="community__grid" role="list">
      {communities.map((item) => (
        <li class="community__item">
          <span class="community__name">{item.name}</span>
          <span class="tag">{item.focus}</span>
        </li>
      ))}
    </ul>

  </div>
</section>

<style>

  .community .section-header__description {
    max-width: min(100%, var(--container-narrow));
  }
  .community__grid {
    list-style: none;
    display: grid;
    grid-template-columns: 1fr;
    gap: var(--space-3);
  }

  @media (min-width: 36em) {
    .community__grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  @media (min-width: 60em) {
    .community__grid {
      grid-template-columns: repeat(3, 1fr);
    }
  }

  .community__item {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: var(--space-4);
    padding: var(--space-4) var(--space-6);
    background-color: var(--color-bg-card);
    border: var(--border-width) solid var(--color-border);
    border-radius: var(--radius-lg);
  }

  .community__name {
    font-size: var(--text-sm);
    font-weight: var(--weight-bold);
    color: var(--color-text-heading);
  }
</style>